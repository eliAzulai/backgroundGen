const array = [1,2,4,591,392,391,2,5,10,2,1,1,1,20,20]

array.sort(function(a, b){return a - b});
console.log(array)
// make a function
function answer(array) {
	const mapArray = array.map((num,i) => {
		return num
	})
} 
// iterate over the array, decide which type probably map
// method that identifies identical type 
toSorted
// create new array for each distinct type 
if array[i] === array[i+1]
// add each distinct type into its own array 

